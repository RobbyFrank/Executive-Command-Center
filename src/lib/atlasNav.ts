/**
 * Fired from the sidebar when the user clicks "Atlas" while already on `/atlas`.
 * {@link PortfolioAtlas} listens and resets to the L0 "all companies" view
 * (same as the Portfolio breadcrumb / Esc to root).
 */
export const ATLAS_RESET_TO_COMPANIES_EVENT = "ecc:atlas-reset-to-companies";
